import React from "react";
import { AppDataConsumer } from "../context/appData";

const Type = () => {
  return (
    <div className="page-content">
      <h2>Type</h2>
    </div>
  );
};

export default AppDataConsumer(Type);
